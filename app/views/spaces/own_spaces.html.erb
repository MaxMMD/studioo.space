<h2 style="text-align:center; padding-top:30px">My Studioos</h2>
<div class="d-flex align-items-start">
  <div class="cards-grid p-3 flex-grow-1">
    <% @spaces.each do |space| %>
      <%= render 'card', space: space %>
    <% end %>
  </div>
</div>
