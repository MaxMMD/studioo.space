<div class="d-flex align-items-start">
  <div class="cards-grid p-3 flex-grow-1">
    <% @spaces.each do |space| %>
      <%= render 'card', space: space %>
    <% end %>
  </div>
  <div
    id="map"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
